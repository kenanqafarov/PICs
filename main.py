import PICs

# getPI(n)
# n - the count of the characters that you want to print
#For example 
eded:int = PICs.getPI(23)
print(eded)

before_crypt = PICs.take("hello123")
print(before_crypt)

after_decrypt = PICs.shoot("-4(tkpdk")
print(after_decrypt)
